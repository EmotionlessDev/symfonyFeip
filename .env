APP_ENV=dev
APP_SECRET=

# Users ids could retrieved by the command `id $USER`
PUID=1000
PGID=1000

# Nginx
NGINX_HOST_HTTP_PORT=8000

# Postgres
POSTGRES_HOST_PORT=5432
POSTGRES_USER=feip
POSTGRES_PASSWORD=feip
POSTGRES_DB=feip
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgresFeip:${POSTGRES_HOST_PORT}/${POSTGRES_DB}?serverVersion=16&charset=utf8"
